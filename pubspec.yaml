name: wishlist_hive_flutter
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^1.4.1+1
  provider:
  path:
  path_provider:
  hive_flutter: ^0.3.0+2




  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  hive_generator:

dependency_overrides:

  path: 1.7.0

flutter:


  uses-material-design: true

